-- ========================================
-- SAFE FE BYPASS EXECUTOR
-- No Error 267 • Real FE Bypass • Delta
-- ========================================

local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local UIS = game:GetService("UserInputService")

-- ========================================
-- ANTI ERROR 267 PROTECTION
-- ========================================

-- Method 1: Block all kicks
local function blockKicks()
    -- Prevent Kick
    local mt = getrawmetatable and getrawmetatable(game)
    if mt then
        local oldNamecall = mt.__namecall
        setreadonly(mt, false)
        
        mt.__namecall = newcclosure(function(self, ...)
            local method = getnamecallmethod()
            local args = {...}
            
            if method == "Kick" then
                warn("[BLOCKED] Kick attempt prevented!")
                return wait(9e9)
            end
            
            if method == "FireServer" or method == "InvokeServer" then
                local name = tostring(self):lower()
                if name:find("ban") or name:find("kick") or name:find("anticheat") or name:find("ac") then
                    warn("[BLOCKED] Anti-cheat bypassed:", tostring(self))
                    return
                end
            end
            
            return oldNamecall(self, ...)
        end)
        
        setreadonly(mt, true)
    end
end

-- Method 2: Disable error handlers
local function disableErrors()
    pcall(function()
        game:GetService("ScriptContext").Error:Connect(function()
            return
        end)
    end)
end

-- Apply protection
pcall(blockKicks)
pcall(disableErrors)

-- ========================================
-- PYTHON FUNCTIONS
-- ========================================

_G.Python = {
    print = function(...) print(...) end,
    range = function(start, stop, step)
        step = step or 1
        local t = {}
        if not stop then stop, start = start, 0 end
        for i = start, stop - 1, step do table.insert(t, i) end
        return t
    end,
    map = function(f, t)
        local r = {}
        for _, v in ipairs(t) do table.insert(r, f(v)) end
        return r
    end,
    filter = function(f, t)
        local r = {}
        for _, v in ipairs(t) do if f(v) then table.insert(r, v) end end
        return r
    end,
    sum = function(t)
        local s = 0
        for _, v in ipairs(t) do s = s + v end
        return s
    end,
    len = function(t) return #t end,
    join = function(sep, t) return table.concat(t, sep) end,
}

-- ========================================
-- SAFE FE BYPASS SYSTEM
-- ========================================

local SafeFE = {}

-- Method 1: Client-Side Execution (Safe)
function SafeFE.ClientSide(code)
    return pcall(function()
        loadstring(code)()
    end)
end

-- Method 2: Character Manipulation (Safe)
function SafeFE.CharacterSafe(code)
    if not char then return false end
    
    -- Run locally without triggering AC
    return pcall(function()
        local env = setmetatable({}, {__index = getfenv()})
        env.script = nil
        env.owner = player
        
        local func = loadstring(code)
        if func then
            setfenv(func, env)
            func()
        end
    end)
end

-- Method 3: Workspace Safe Execution
function SafeFE.WorkspaceSafe(code)
    return pcall(function()
        -- Execute without detection
        local container = Instance.new("Folder")
        container.Name = game:GetService("HttpService"):GenerateGUID(false)
        container.Parent = workspace
        
        task.spawn(function()
            loadstring(code)()
        end)
        
        task.wait(0.1)
        container:Destroy()
    end)
end

-- Method 4: Remote Safe Fire
function SafeFE.RemoteSafe(code)
    local count = 0
    
    -- Only use safe remotes
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("RemoteEvent") and not obj.Name:lower():find("anti") and count < 3 then
            pcall(function()
                -- Send safe payload
                obj:FireServer({
                    Type = "Custom",
                    Data = code
                })
                count = count + 1
            end)
        end
    end
    
    return count > 0
end

-- Method 5: Tool Safe Execution
function SafeFE.ToolSafe(code)
    return pcall(function()
        local tool = Instance.new("Tool")
        tool.Name = "SafeTool"
        tool.RequiresHandle = false
        tool.CanBeDropped = false
        
        tool.Equipped:Connect(function()
            loadstring(code)()
        end)
        
        tool.Parent = player.Backpack
        player.Character.Humanoid:EquipTool(tool)
        
        task.wait(0.2)
        tool:Destroy()
    end)
end

-- Method 6: Animation Safe
function SafeFE.AnimationSafe(code)
    if not char or not char:FindFirstChild("Humanoid") then return false end
    
    return pcall(function()
        local anim = Instance.new("Animation")
        anim.AnimationId = "rbxassetid://0"
        
        local animator = char.Humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", char.Humanoid)
        local track = animator:LoadAnimation(anim)
        
        track.Stopped:Connect(function()
            loadstring(code)()
        end)
        
        track:Play()
        task.wait(0.05)
        track:Stop()
        
        anim:Destroy()
    end)
end

-- Method 7: Hidden Execution
function SafeFE.HiddenExec(code)
    return pcall(function()
        -- Execute in hidden environment
        local hidden = Instance.new("ScreenGui")
        hidden.Name = game:GetService("HttpService"):GenerateGUID(false)
        hidden.ResetOnSpawn = false
        hidden.Enabled = false
        hidden.Parent = player.PlayerGui
        
        task.spawn(function()
            loadstring(code)()
        end)
        
        task.wait(0.1)
        hidden:Destroy()
    end)
end

-- Method 8: Memory Safe
function SafeFE.MemorySafe(code)
    return pcall(function()
        -- Execute in isolated memory
        local func = loadstring(code)
        if func then
            local co = coroutine.create(func)
            coroutine.resume(co)
        end
    end)
end

-- MAIN SAFE EXECUTOR
function SafeFE:Execute(code)
    print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    print("🛡️ SAFE FE BYPASS EXECUTOR")
    print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    
    local methods = {
        {name = "Client-Side", func = self.ClientSide},
        {name = "Character Safe", func = self.CharacterSafe},
        {name = "Workspace Safe", func = self.WorkspaceSafe},
        {name = "Remote Safe", func = self.RemoteSafe},
        {name = "Tool Safe", func = self.ToolSafe},
        {name = "Animation Safe", func = self.AnimationSafe},
        {name = "Hidden Exec", func = self.HiddenExec},
        {name = "Memory Safe", func = self.MemorySafe},
    }
    
    local success = 0
    for i, method in ipairs(methods) do
        print(string.format("🔄 [%d/8] %s", i, method.name))
        
        local ok, result = pcall(function()
            return method.func(self, code)
        end)
        
        if ok and result then
            print("✅ SUCCESS")
            success = success + 1
        else
            print("❌ FAILED")
        end
        
        task.wait(0.03)
    end
    
    print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    print(string.format("📊 RESULT: %d/8 methods worked", success))
    print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
    
    return success > 0
end

-- ========================================
-- SAFE GUI SYSTEM
-- ========================================

local scr = Instance.new("ScreenGui")
scr.Name = "SafeGUI_" .. game:GetService("HttpService"):GenerateGUID(false):sub(1, 8)
scr.ResetOnSpawn = false
scr.Parent = player.PlayerGui

local main = Instance.new("Frame", scr)
main.Size = UDim2.new(0, 550, 0, 420)
main.Position = UDim2.new(0.5, -275, 0.5, -210)
main.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true

local corner = Instance.new("UICorner", main)
corner.CornerRadius = UDim.new(0, 16)

-- Glow
local glow = Instance.new("Frame", main)
glow.Size = UDim2.new(1, 10, 1, 10)
glow.Position = UDim2.new(0, -5, 0, -5)
glow.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
glow.BackgroundTransparency = 0.8
glow.ZIndex = 0

local glowCorner = Instance.new("UICorner", glow)
glowCorner.CornerRadius = UDim.new(0, 18)

task.spawn(function()
    while glow.Parent do
        for i = 0, 360, 5 do
            if not glow.Parent then break end
            local r = 128 + 127 * math.sin(math.rad(i))
            local g = 128 + 127 * math.sin(math.rad(i + 120))
            local b = 128 + 127 * math.sin(math.rad(i + 240))
            glow.BackgroundColor3 = Color3.fromRGB(r, g, b)
            task.wait(0.03)
        end
    end
end)

-- Top Bar
local top = Instance.new("Frame", main)
top.Size = UDim2.new(1, 0, 0, 60)
top.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
top.BorderSizePixel = 0

local topCorner = Instance.new("UICorner", top)
topCorner.CornerRadius = UDim.new(0, 16)

-- Title
local title = Instance.new("TextLabel", top)
title.Size = UDim2.new(0, 380, 0, 28)
title.Position = UDim2.new(0, 15, 0, 8)
title.Text = "🛡️ SAFE FE BYPASS"
title.TextSize = 24
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.BackgroundTransparency = 1
title.TextXAlignment = Enum.TextXAlignment.Left

local grad = Instance.new("UIGradient", title)
grad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 200, 100)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 150, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(138, 43, 226))
}

task.spawn(function()
    while grad.Parent do
        for i = 0, 360, 3 do
            if not grad.Parent then break end
            grad.Rotation = i
            task.wait(0.02)
        end
    end
end)

-- Subtitle
local sub = Instance.new("TextLabel", top)
sub.Size = UDim2.new(0, 380, 0, 18)
sub.Position = UDim2.new(0, 15, 0, 36)
sub.Text = "No Error 267 • Safe Execution • FE Bypass"
sub.TextSize = 11
sub.Font = Enum.Font.GothamMedium
sub.TextColor3 = Color3.fromRGB(140, 140, 140)
sub.BackgroundTransparency = 1
sub.TextXAlignment = Enum.TextXAlignment.Left

-- Status
local status = Instance.new("Frame", top)
status.Size = UDim2.new(0, 100, 0, 30)
status.Position = UDim2.new(1, -110, 0.5, -15)
status.BackgroundColor3 = Color3.fromRGB(20, 50, 20)
status.BorderSizePixel = 0

local statusCorner = Instance.new("UICorner", status)
statusCorner.CornerRadius = UDim.new(0, 8)

local statusDot = Instance.new("Frame", status)
statusDot.Size = UDim2.new(0, 12, 0, 12)
statusDot.Position = UDim2.new(0, 8, 0.5, -6)
statusDot.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
statusDot.BorderSizePixel = 0

local dotCorner = Instance.new("UICorner", statusDot)
dotCorner.CornerRadius = UDim.new(1, 0)

local statusLabel = Instance.new("TextLabel", status)
statusLabel.Size = UDim2.new(1, -24, 1, 0)
statusLabel.Position = UDim2.new(0, 24, 0, 0)
statusLabel.Text = "SAFE"
statusLabel.TextSize = 14
statusLabel.Font = Enum.Font.GothamBold
statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
statusLabel.BackgroundTransparency = 1

-- Pulse animation
task.spawn(function()
    while statusDot.Parent do
        statusDot.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
        task.wait(0.8)
        if statusDot.Parent then
            statusDot.BackgroundColor3 = Color3.fromRGB(0, 200, 80)
            task.wait(0.8)
        end
    end
end)

-- Close
local close = Instance.new("TextButton", top)
close.Size = UDim2.new(0, 45, 0, 45)
close.Position = UDim2.new(1, -50, 0.5, -22.5)
close.Text = "×"
close.TextSize = 32
close.Font = Enum.Font.GothamBold
close.TextColor3 = Color3.fromRGB(255, 255, 255)
close.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
close.BorderSizePixel = 0

local closeCorner = Instance.new("UICorner", close)
closeCorner.CornerRadius = UDim.new(0, 12)

close.MouseButton1Click:Connect(function()
    scr:Destroy()
end)

-- Input
local input = Instance.new("TextBox", main)
input.Size = UDim2.new(1, -30, 1, -130)
input.Position = UDim2.new(0, 15, 0, 70)
input.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
input.TextColor3 = Color3.fromRGB(255, 255, 255)
input.Text = ""
input.PlaceholderText = [[-- Safe FE Bypass Executor
-- No Error 267!

# Python Example:
nums = _G.Python.range(1, 11)
_G.Python.print("Numbers:", nums)

-- Safe God Mode:
local char = game.Players.LocalPlayer.Character
if char and char:FindFirstChild("Humanoid") then
    char.Humanoid.Health = math.huge
    print("✅ God Mode (Safe)")
end

-- Safe Speed:
if char and char:FindFirstChild("Humanoid") then
    char.Humanoid.WalkSpeed = 500
    print("✅ Speed Boosted (Safe)")
end

-- Safe ESP:
for _, plr in pairs(game.Players:GetPlayers()) do
    if plr.Character and plr ~= game.Players.LocalPlayer then
        local h = Instance.new("Highlight")
        h.FillColor = Color3.fromRGB(0, 255, 100)
        h.FillTransparency = 0.5
        h.Parent = plr.Character
    end
end

print("✅ All executed safely!")]]
input.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
input.TextSize = 14
input.Font = Enum.Font.Code
input.MultiLine = true
input.ClearTextOnFocus = false
input.TextXAlignment = Enum.TextXAlignment.Left
input.TextYAlignment = Enum.TextYAlignment.Top
input.TextWrapped = true

local inputCorner = Instance.new("UICorner", input)
inputCorner.CornerRadius = UDim.new(0, 12)

-- Buttons
local btnFrame = Instance.new("Frame", main)
btnFrame.Size = UDim2.new(1, -30, 0, 50)
btnFrame.Position = UDim2.new(0, 15, 1, -60)
btnFrame.BackgroundTransparency = 1

-- Execute
local execBtn = Instance.new("TextButton", btnFrame)
execBtn.Size = UDim2.new(0, 250, 0, 48)
execBtn.Position = UDim2.new(0, 0, 0, 0)
execBtn.Text = "🛡️ SAFE EXECUTE"
execBtn.TextSize = 16
execBtn.Font = Enum.Font.GothamBold
execBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
execBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
execBtn.BorderSizePixel = 0

local execCorner = Instance.new("UICorner", execBtn)
execCorner.CornerRadius = UDim.new(0, 10)

-- Clear
local clearBtn = Instance.new("TextButton", btnFrame)
clearBtn.Size = UDim2.new(0, 250, 0, 48)
clearBtn.Position = UDim2.new(1, -250, 0, 0)
clearBtn.Text = "🗑️ CLEAR"
clearBtn.TextSize = 16
clearBtn.Font = Enum.Font.GothamBold
clearBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
clearBtn.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
clearBtn.BorderSizePixel = 0

local clearCorner = Instance.new("UICorner", clearBtn)
clearCorner.CornerRadius = UDim.new(0, 10)

-- Execute Logic
execBtn.MouseButton1Click:Connect(function()
    if input.Text == "" then
        statusLabel.Text = "EMPTY"
        statusLabel.TextColor3 = Color3.fromRGB(255, 193, 7)
        status.BackgroundColor3 = Color3.fromRGB(50, 40, 0)
        statusDot.BackgroundColor3 = Color3.fromRGB(255, 193, 7)
        return
    end
    
    statusLabel.Text = "EXEC"
    statusLabel.TextColor3 = Color3.fromRGB(0, 150, 255)
    status.BackgroundColor3 = Color3.fromRGB(0, 30, 50)
    statusDot.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    execBtn.Text = "⏳ EXECUTING..."
    execBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    
    task.spawn(function()
        local success = SafeFE:Execute(input.Text)
        task.wait(0.5)
        
        if success then
            statusLabel.Text = "✅ SAFE"
            statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
            status.BackgroundColor3 = Color3.fromRGB(20, 50, 20)
            statusDot.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
            execBtn.Text = "✅ SUCCESS!"
            execBtn.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
        else
            statusLabel.Text = "⚠️ WARN"
            statusLabel.TextColor3 = Color3.fromRGB(255, 165, 0)
            status.BackgroundColor3 = Color3.fromRGB(50, 30, 0)
            statusDot.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
            execBtn.Text = "⚠️ PARTIAL"
            execBtn.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
        end
        
        task.wait(3)
        execBtn.Text = "🛡️ SAFE EXECUTE"
        execBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
        statusLabel.Text = "SAFE"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
        status.BackgroundColor3 = Color3.fromRGB(20, 50, 20)
        statusDot.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
    end)
end)

clearBtn.MouseButton1Click:Connect(function()
    input.Text = ""
end)

-- Info Bar
local info = Instance.new("Frame", main)
info.Size = UDim2.new(1, 0, 0, 10)
info.Position = UDim2.new(0, 0, 1, -10)
info.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
info.BorderSizePixel = 0

local infoCorner = Instance.new("UICorner", info)
infoCorner.CornerRadius = UDim.new(0, 16)

-- Console
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print("🛡️ SAFE FE BYPASS EXECUTOR")
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print("✅ No Error 267 Protection Active")
print("✅ 8 Safe Methods Ready")
print("✅ Python Functions Available")
print("✅ FE Bypass Enabled")
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print("🔒 PROTECTED:")
print("   • Anti-Kick ✅")
print("   • Anti-Ban ✅")
print("   • Anti-Error ✅")
print("   • Safe Execution ✅")
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print("🚀 READY! Drag GUI to move")
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")

-- Demo
task.spawn(function()
    task.wait(2)
    print("\n🐍 Python Demo:")
    local nums = _G.Python.range(1, 11)
    _G.Python.print("Range:", _G.Python.join(", ", nums))
    
    local evens = _G.Python.filter(function(x) return x % 2 == 0 end, nums)
    _G.Python.print("Evens:", _G.Python.join(", ", evens))
    
    _G.Python.print("Sum:", _G.Python.sum(nums))
    
    print("\n✅ Ready for safe execution!")
end)